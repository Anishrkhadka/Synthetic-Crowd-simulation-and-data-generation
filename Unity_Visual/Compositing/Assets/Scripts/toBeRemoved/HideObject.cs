using UnityEngine;

public class HideObject : MonoBehaviour
{
    // Use this for initialization
    private void Start()
    {
        gameObject.layer = 1;
    }

    // Update is called once per frame
    private void Update()
    {
    }
}